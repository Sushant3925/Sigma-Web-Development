import React from 'react'
import Button from './Button'
const Navbar = (props) => {
  return (
    <div>
      Navbar
      {/* <Button count={props.count}/> */}
      <Button/>
    </div>
  )
}

export default Navbar
